# Auth-managing reverse proxy to the external payment provider.
# It injects a self-managed upstream token (see topology.yml) — clients send
# only the inbound proxy key.
STRIPE_API_BASE=https://api.stripe.com
PROXY_API_KEY=pk_inbound_changeme
